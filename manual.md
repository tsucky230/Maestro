# Maestro 利用マニュアル

> 🎼 このマニュアルを読めば、AIエージェントと一緒に品質の高いソフトウェア開発ができるようになります。
> プログラミング経験が浅くても大丈夫です。Maestroがあなたをガイドします。

---

## はじめに — Maestroとは？

Maestroは「**AIエージェントと一緒にソフトウェアを作るためのルールブック**」です。

普通にAIに「コード書いて」と頼むと、動くコードは出てきますが、以下のような問題が起きがちです:

- なぜそう作ったのか、後から分からない
- テストが抜けていて、後でバグが見つかる
- 仕様変更の影響範囲が分からない
- 前任者が何をしたのか分からない

Maestroはこれらの問題を防ぐために、**AIに「プロの開発プロセス」を教え込んだもの**です。AIがプロセスに従って仕事するので、あなたは品質を心配せず、**「何を作りたいか」に集中できます**。

### 使えるAIエージェント

| エージェント | 起動方法 |
|---|---|
| **Antigravity** | `/develop` と入力 |
| **GitHub Copilot** | Copilot Chatで質問 |
| **Claude Code** | Claude Codeで質問 |
| **Codex** | Codexで質問 |

どのエージェントを使っても、同じプロセス・同じ品質で仕事してくれます。

---

## 🌿 最初に知っておくこと — ブランチのルール

Maestroには1つだけ**絶対に守るべきルール**があります:

> **mainブランチでは作業しない。必ず作業用ブランチを作ってから始める。**

mainブランチはMaestroフレームワーク自体（ルールやテンプレート）を管理する場所です。あなたのプロジェクトの成果物はすべて別のブランチで管理します。

なぜこのルールがあるのか？

- 失敗しても安心（ブランチを消せばいい）
- 複数のプロジェクトを並行して進められる
- 作業結果をチームメンバーにレビューしてもらいやすい

**心配しないでください。ブランチの作成はエージェントが自動でやってくれます。**

---

## 📋 エージェントを起動すると何が起きるか

エージェントに話しかけると、まず以下のメニューが表示されます:

```
🎼 Maestro — ESPR 2.0 ソフトウェア開発エージェント

何をお手伝いしましょうか？

1️⃣  新規プロジェクトを開始する（要求ヒアリングから）
2️⃣  既存プロジェクトの作業を継続する
3️⃣  成果物をレビューする（設計書、コード等）
4️⃣  成果物間の整合性をチェックする
5️⃣  テストを設計・実行する
6️⃣  既存の成果物を閲覧・検索する
7️⃣  フレームワーク自体を更新する（テンプレート、ルール等）
```

番号を選ぶか、やりたいことを自由に伝えてください。ここからは、よくあるユースケースごとに手順を説明します。

---

## 📖 ユースケース1: 外部から受け取った仕様書をレビューしたい

### こんなとき

- 顧客や上流工程から仕様書が届いた
- 「これで問題ないか確認して」と言われた
- 仕様に抜け漏れや矛盾がないかチェックしたい

### 手順

**ステップ1: エージェントに依頼する**

```
外部から届いた仕様書をレビューしたいです。
```

エージェントはメニューの **3️⃣ 成果物をレビューする** を選択します。

**ステップ2: レビュー対象を伝える**

```
この仕様書をレビューしてください:（ファイルパスを貼る or 内容を貼る）
```

**ステップ3: エージェントがレビューを実施**

エージェントは自動的に以下のことをやってくれます:

1. レビュー用ブランチを作成（`review/プロジェクト名/仕様レビュー`）
2. レビュー観点表（`knowledge/review-perspectives.md`）に基づいてチェック
3. **曖昧語の検出**（「適切に」「必要に応じて」「等」→ 具体化を提案）
4. 指摘事項に深刻度を付与:
   - 🔴 Critical — 必ず修正
   - 🟠 Major — 修正推奨
   - 🟡 Minor — 改善提案
   - 🔵 Info — 情報共有

**ステップ4: 結果を確認する**

レビュー結果がレポートとして出力されます。指摘の例:

```
🔴 REQ-MISS: 「ユーザー認証」の異常系（パスワード誤り時の挙動）が未定義です。
🟠 SPEC-AMB: 「高速に処理する」→ 具体的な応答時間（例: 200ms以内）を定義すべきです。
🟡 DOC-ERR: 3.2節と5.1節で「ユーザーID」と「利用者番号」が混在しています。
```

**ステップ5: 結果をコミッ卜する**

変更がまとまったら、OKだと伝えるとエージェントがコミットしてくれます。

### ポイント

- レビュー観点は `knowledge/review-perspectives.md` に蓄積されているので、プロジェクトを重ねるほど精度が上がります
- 仕様書同士の矛盾チェックもできます（→ ユースケース4参照）

---

## 📖 ユースケース2: 外部から受け取ったソースコードをレビューしたい

### こんなとき

- 協力会社からコードが納品された
- チームメンバーのプルリクエストをレビューしたい
- セキュリティの問題がないか確認したい

### 手順

**ステップ1: エージェントに依頼する**

```
このソースコードをレビューしてください:（ファイルパスやディレクトリを指定）
```

**ステップ2: エージェントがチェックすること**

エージェントは以下の観点で自動的にレビューします:

| チェック項目 | 何を見ているか |
|---|---|
| コーディング規約 | 命名規則、関数の長さ、コメントの有無 |
| エラー処理 | 例外ハンドリング、入力バリデーション |
| セキュリティ | SQLインジェクション、XSS、秘密情報のハードコード |
| テスト | テストの有無、カバレッジの見通し |
| 設計 | 依存関係の方向、循環依存、複雑度 |

**ステップ3: 結果を受け取る**

指摘には分類コードが付きます:

```
🔴 SEC-VUL: パスワードがログ出力に含まれています（auth.py:45行目）
🟠 COD-BUG: nullチェックなしで配列にアクセスしています（util.py:23行目）
🟡 COD-STD: 関数名が命名規約に違反しています（getData → get_data）
```

### ウォークスルーを依頼する

より詳しいレビューをしたい場合:

```
このコードのウォークスルーをしてください。
```

エージェントは以下の形式で構造的に説明してくれます:

1. **概要** — コードの目的と全体構造
2. **設計判断のポイント** — なぜこう作ったのか
3. **リスクと懸念** — 問題になりそうな箇所
4. **改善提案** — こうすると良くなる

---

## 📖 ユースケース3: ゼロからソフトウェアを作る

### こんなとき

- 新しいアプリケーションを開発することになった
- まだ何も決まっていないが、アイデアがある
- 品質の高いプロセスで開発を進めたい

### 全体の流れ

Maestroは「V字モデル」という開発プロセスに従います。難しく聞こえますが、要は**「作る前にちゃんと考え、作った後にちゃんと確認する」**ということです。

```
① 何を作るか決める（要求仕様）  ←→  ⑥ 全体テスト
② どう作るか決める（方式設計）   ←→  ⑤ 結合テスト
③ 細かく決める（詳細設計）      ←→  ④ 単体テスト
                    ↓
               コードを書く
```

### ステップ1: 要求をヒアリングしてもらう

```
新しいプロジェクトを始めたいです。社内の勤怠管理システムを作ります。
```

エージェントは以下のように質問してきます:

```
プロジェクト名は「勤怠管理システム」でよいですか？

では、いくつか質問させてください:

1. このシステムを使うのは誰ですか？（一般社員？管理職？人事部？）
2. 必要な機能は何ですか？（出勤/退勤打刻、残業申請、有給管理 等）
3. 動作環境は？（Webアプリ？スマホアプリ？社内ネットワーク限定？）
4. セキュリティや性能の要件はありますか？
```

あなたの回答をもとに、エージェントは **要求仕様書** を作ってくれます。この仕様書は「USDM」という形式で書かれますが、読み方は簡単です:

```
【要求】REQ-001: 社員は出勤・退勤の時刻を記録できること
  【理由】日々の出退勤時刻を正確に管理し、給与計算の基礎データとするため
  【仕様】SPEC-001: 打刻ボタンを押すと現在時刻がサーバーに記録される
  【仕様】SPEC-002: 記録精度は分単位とする（秒は切り捨て）
  【仕様】SPEC-003: 同日に2回以上の出勤打刻はエラーとする
```

> 💡 **ここが大事**: エージェントは自動的に「曖昧な表現」を検出します。「高速に処理」「適切に対応」のような表現はNGです。必ず数値で定義します（例: 応答時間200ms以内）。

### ステップ2: 設計する

要求仕様がOKなら、次は設計です:

```
要求仕様をもとに方式設計に進んでください。
```

エージェントは以下を自動的に作成します:

- **コンポーネント図** — システムの構成要素を図示（Mermaid図）
- **シーケンス図** — 処理の流れを図示
- **ER図** — データベースの構造
- **FMEA** — 「壊れるとしたらどこか？」を分析するリスク表
- **設計制約（ADR）** — 守るべきルールの明文化

### ステップ3: 詳細設計からコーディング

```
詳細設計に進んでください。
```

エージェントは各関数に対して **契約** を書きます:

```
前提条件: employee_id は正の整数であること
事後条件: 戻り値はTimestamp型で、現在時刻±1秒以内であること
例外: employee_id が存在しない場合 → EmployeeNotFoundError
```

これがあるから、テストケースを自動で導き出せるのです。

### ステップ4〜6: テストする

```
単体テストを設計してください。
```

テストは自動的に以下の観点で作られます:

- 前提条件の境界値（0, -1, 最大値）
- 事後条件が満たされるか
- 状態遷移表の全パスを通るか
- 異常系（エラーが正しく発生するか）

### 途中でOKを出すと…

```
あなた: OK
エージェント: （CHANGELOG.mdを更新してコミット）
```

成果物が作業用ブランチにコミットされます。失敗してもmainブランチは無傷です。

---

## 📖 ユースケース4: 仕様書と設計書の矛盾を見つけたい

### こんなとき

- 仕様書が更新されたが、設計書がそのままになっている気がする
- 複数の文書間で数値や用語がバラバラになっている不安がある

### 手順

```
仕様書と設計書の整合性をチェックしてください。
```

エージェントはメニューの **4️⃣ 成果物間の整合性をチェックする** を選択し、以下の観点で自動分析します:

| チェック観点 | 何を見るか |
|---|---|
| 用語の統一 | 同じものに違う名前がついていないか |
| 数値の一致 | 応答時間、データサイズ等が文書間で一致しているか |
| 機能の漏れ | 仕様書にある機能が設計書に全部含まれているか |
| 非機能要求 | セキュリティや性能の要件が設計に反映されているか |
| タグの連鎖 | REQ→SPEC→ARC→DET の追跡が途切れていないか |

結果はレポートとして出力されます:

```
❌ 不整合: 仕様書ではAPI応答時間「200ms以内」、設計書では「500ms以内」
⚠️ 用語不統一: 仕様書「利用者」= 設計書「ユーザー」（統一推奨）
✅ 機能カバレッジ: 全12機能が設計書に含まれています
```

---

## 📖 ユースケース5: トレーサビリティを確認したい

### こんなとき

- この機能はどの要求から来ているの？
- この要求はテストで検証されているの？
- 仕様変更したら、どこに影響があるの？

### トレーサビリティとは

Maestroでは、すべての成果物に**タグ**が付いています。このタグが鎖のようにつながっています:

```
REQ-001（要求）
  → SPEC-001（仕様）
    → ARC-001（方式設計）
      → DET-001（詳細設計）
        → SRC-001（ソースコード）
        → UT-001（単体テスト）
      → IT-001（結合テスト）
    → QT-001（適格性確認テスト）
```

### 確認方法

**方法1: 前方追跡（要求 → テスト）**

```
REQ-001 に関連するすべての成果物を追跡してください。
```

結果:
```
REQ-001: 社員は出勤・退勤の時刻を記録できること
  ├── SPEC-001: 打刻ボタン → サーバー記録
  │     ├── ARC-001: 認証サービス → 打刻サービス → DBコネクタ
  │     │     ├── DET-001: clock_in(employee_id) 関数
  │     │     │     ├── SRC-001: attendance.py:15
  │     │     │     └── UT-001: test_clock_in_success
  │     │     └── IT-001: 認証→打刻→DB記録の結合テスト
  │     └── QT-001: 出勤打刻シナリオテスト
  ├── SPEC-002: 分単位精度
  └── SPEC-003: 重複打刻エラー
```

**方法2: 後方追跡（テスト → 要求）**

```
UT-005 はどの要求を検証していますか？
```

**方法3: 影響分析**

```
REQ-001 を変更した場合、影響範囲を教えてください。
```

エージェントは関連するすべての設計・コード・テストをリストアップします。

**方法4: トレーサビリティマトリクスの作成**

```
トレーサビリティマトリクスを作成してください。
```

全タグの対応関係が一覧表で出力されます。抜け漏れがあれば警告が出ます。

---

## 📖 ユースケース6: 既存プロジェクトの作業を再開する

### こんなとき

- 昨日の続きをやりたい
- 前任者から引き継いだプロジェクトを再開したい
- しばらく放置していたプロジェクトを再開したい

### 手順

```
既存プロジェクトの作業を続けたいです。
```

エージェントはメニューの **2️⃣ 既存プロジェクトの作業を継続する** を選択し:

1. `docs/projects/` 配下のプロジェクト一覧を表示
2. あなたがプロジェクトを選択
3. コンテキストファイル（`current-phase.md`）を読んで今どの段階かを報告
4. 過去の対話ログの要約を示す
5. 作業用ブランチをチェックアウト

```
📋 プロジェクト「勤怠管理」の状況:
  現在の工程: SWP.3（詳細設計）
  進捗: 12/15機能の詳細設計が完了
  最後の作業: 2025-02-13 — 打刻機能の状態遷移表作成
  未解決事項: ログイン失敗時のロック解除方法が未決
  
  どこから再開しますか？
```

### ポイント

- **対話ログ**が残っているので、前回の議論の流れを確認できます
- **コンテキストファイル**に判断の履歴が残っているので、「なぜこう決めたか」が分かります
- 前任者がいなくても、対話ログとコンテキストファイルがすべてを教えてくれます

---

## 📖 ユースケース7: プロジェクト完了時の振り返り

### こんなとき

- プロジェクトがひと段落した
- 今回の反省を次に活かしたい
- チーム全体の品質を底上げしたい

### 手順

```
プロジェクトのレトロスペクティブ（振り返り）をしたいです。
```

エージェントは以下を自動で集計・分析します:

1. **Keep** — うまくいったこと
2. **Problem** — 問題点
3. **Try** — 次回試すこと
4. **メトリクスサマリー** — 品質の数値データ
5. **欠陥分析** — どの工程でバグが混入し、どの工程で発見されたか

特に重要なのが **欠陥分析** です:

```
😰 要求仕様の段階で混入したバグが、結合テストまで発見されなかった
   → ⚡ 改善策: 要求レビューのチェック項目を追加
```

この知見は自動的に `knowledge/` に蓄積されるので、次のプロジェクトでは同じ失敗を避けられます。

---

## 💡 日常的に使えるTips

### 「とりあえず見せて」

成果物を提出する前に、エージェントは自動的に **自己評価** を実行します。結果はこんな風に表示されます:

```
📊 自己評価レポート
━━━━━━━━━━━━━━━━━━
✅ 通過: 8/10 項目
⚠️ 要確認: 1 項目（結合度が閾値に微超過）
❌ 未達: 1 項目（FMEA未作成）
━━━━━━━━━━━━━━━━━━
```

品質に問題がある場合はエージェントが自分で修正を試みます（最大3回）。それでもダメな場合はあなたに相談が来ます。

### 「曖昧な仕様を書いてしまう」

心配いりません。エージェントは以下のような曖昧な表現を自動で検出して警告します:

```
❌ 「高速に処理する」→ 具体的な数値を: 「応答時間200ms以内とする」
❌ 「適切にエラー処理する」→ 具体的に: 「404エラーの場合、ユーザーにダイアログで通知する」
❌ 「ユーザー情報等を保存する」→ 「等」を使わず全項目を列挙してください
```

### 「OKと言えばコミットしてくれる」

変更のたびに:
1. エージェントが変更内容を報告
2. あなたが **OK** と言う
3. エージェントが `CHANGELOG.md` を更新してコミット

これにより、すべての変更が追跡可能になります。

### 「変更したら他のファイルとの整合性は？」

エージェントは `dependency-map.md` という依存関係マップを持っています。ファイルを変更すると、このマップを参照して **影響を受けるファイルの該当部分だけ** をチェックします。全ファイルを毎回読み直す無駄はありません。

```
変更: AGENTS.md Section 21（メトリクス閾値変更）
  → quality-metrics.md の閾値を確認 ✅
  → self-evaluation.md のメトリクス参照を確認 ✅
  → develop.md の品質ゲートルールを確認 ✅
```

あなたが何もしなくても、エージェントが自動的に整合性を保ってくれます。

### 「何を聞けばいいか分からない」

困ったら以下のフレーズが使えます:

| やりたいこと | 言い方 |
|---|---|
| 全体像を知りたい | 「このプロジェクトの現状を教えて」 |
| 品質を確認したい | 「メトリクスを見せて」 |
| リスクを知りたい | 「FMEAの結果を見せて」 |
| 影響範囲を知りたい | 「REQ-001を変更したら何に影響する？」 |
| 過去の経緯を知りたい | 「この設計判断の理由は？」 |
| テスト状況を知りたい | 「テストカバレッジはどのくらい？」 |
| 前回の続きから | 「前回の続きから」 |

---

## 🗂️ ファイル構成を理解する

Maestroのファイルは、大きく3つの役割に分かれます:

### 1. ルール（触らなくてOK）

エージェントが自動的に読み込むファイルです。あなたが直接編集する必要はありません:

| ファイル | 内容 |
|---|---|
| `AGENTS.md` | 全ルールの本体（28セクション） |
| `dependency-map.md` | ファイル間の依存関係マップ（変更管理用） |
| `CLAUDE.md` | Claude Code用の設定 |
| `.github/copilot-instructions.md` | GitHub Copilot用の設定 |
| `.agent/workflows/develop.md` | Antigravity用のワークフロー |

### 2. テンプレート（エージェントが使う）

エージェントが成果物を作るときに使うひな形です:

| ファイル | 用途 |
|---|---|
| `docs/templates/requirements-spec.md` | 要求仕様書 |
| `docs/templates/architecture-design.md` | 方式設計書 |
| `docs/templates/detailed-design.md` | 詳細設計書 |
| `docs/templates/test-design.md` | テスト設計書 |
| `docs/templates/fmea-fta.md` | リスク分析（FMEA/FTA） |
| `docs/templates/traceability-matrix.md` | トレーサビリティマトリクス |
| `docs/templates/consistency-review.md` | 整合性レビュー |
| `docs/templates/conversation-log.md` | 対話ログ |
| `docs/templates/retrospective.md` | 振り返り |

### 3. 知見（使うほど育つ）

過去のプロジェクトの経験が蓄積される場所です:

| ファイル | 内容 |
|---|---|
| `knowledge/review-perspectives.md` | レビューで何をチェックするか |
| `knowledge/checklists.md` | 工程完了のチェックリスト |
| `knowledge/trouble-cases.md` | 過去のトラブル事例 |
| `knowledge/quality-metrics.md` | 品質メトリクスの定義と基準値 |
| `knowledge/self-evaluation.md` | 自己評価のチェック項目 |
| `knowledge/patterns.md` | 成功パターン集 |

---

## ❓ よくある質問

### Q: どのエージェントを使えばいいですか？

A: どれでも同じ品質で作業してくれます。普段使い慣れているものを使ってください。Antigravityを使う場合は `/develop` コマンドでワークフローが始まります。

### Q: 英語のプロジェクトでも使えますか？

A: はい。Maestroの規約は日本語で書かれていますが、成果物は日本語でも英語でも作成できます。

### Q: AGENTS.md を全部読む必要がありますか？

A: いいえ。**このマニュアルを読めば十分です。** AGENTS.md はエージェントが読むためのファイルです。あなたが直接参照する必要はありません。

### Q: 小さなスクリプトでも全プロセスを踏む必要がありますか？

A: いいえ。プロジェクトの規模に応じてエージェントに伝えてください。「シンプルなツールなので軽量に進めてほしい」と言えば、エージェントは適切に調整します。

### Q: エージェントが間違ったことを言ったらどうすれば？

A: 遠慮なく指摘してください。エージェントは指摘を受けて修正します。すべての対話は対話ログに記録されるので、後から「なぜ修正したか」も追跡できます。

### Q: mainブランチを間違って変更してしまったら？

A: `git stash` や `git reset` で元に戻せます。分からなければエージェントに「mainブランチの変更を取り消してください」と頼めば対応してくれます。

---

## 🎯 まとめ

1. **エージェントを起動** → メニューからやりたいことを選ぶ
2. **ブランチは自動作成** → mainブランチは触らない
3. **やりたいことを自由に伝える** → エージェントがプロセスに従って進める
4. **OKと言えばコミット** → 変更履歴が自動で残る
5. **困ったら聞く** → 過去の対話ログ・知見がすべて残っている

Maestroを使えば、プログラミング初心者でも、ベテランエンジニアと同じ品質基準で開発を進められます。最初は慣れないかもしれませんが、エージェントがガイドしてくれるので、安心して始めてください。

**さあ、エージェントに「新しいプロジェクトを始めたいです」と言ってみましょう！** 🎼
